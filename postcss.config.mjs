// Local PostCSS config so a Tailwind/PostCSS config higher up the directory
// tree is not applied to this project. This app uses plain CSS only.
const config = {
  plugins: {},
};

export default config;
